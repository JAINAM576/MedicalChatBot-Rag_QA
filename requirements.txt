# ─────────────────────────────────────────────────────────────────
# Inference-only dependencies for MedicalChatBot FastAPI backend
# Python 3.10.x  |  torch installed CPU-only separately in Dockerfile
# ─────────────────────────────────────────────────────────────────

# ── Web Framework ───────────────────────────────────────────────
fastapi==0.126.0
uvicorn==0.31.1
jinja2==3.1.6
python-multipart==0.0.22

# ── LangChain Stack ─────────────────────────────────────────────
langchain==1.2.15
langchain-classic==1.0.4
langchain-community==0.4.1
langchain-core==1.3.2
langchain-groq==1.1.1
langchain-pinecone==0.2.0
langchain-text-splitters==1.1.2
langsmith==0.7.37

# ── Vector Database (Pinecone) ───────────────────────────────────
pinecone==6.0.1

# ── LLM Client (Groq) ───────────────────────────────────────────
groq==0.37.1

# ── Embeddings (HuggingFace) ─────────────────────────────────────
# torch (CPU-only) is installed separately in the Dockerfile
sentence-transformers==3.4.1
transformers==4.49.0
huggingface-hub==0.29.2
tokenizers==0.21.0
safetensors==0.5.3

# ── Data & PDF ───────────────────────────────────────────────────
pypdf==5.3.1
pandas==2.2.3
numpy==2.2.6

# ── HTTP & Auth ──────────────────────────────────────────────────
httpx==0.28.1
requests==2.33.1

# ── Utilities ────────────────────────────────────────────────────
python-dotenv==1.0.0
tiktoken==0.9.0
tenacity==9.1.4
pydantic==2.13.3
pydantic_core==2.46.3


fastapi
jinja2
langchain
langchain-classic
langchain-community
langchain-groq
langchain-openai
langchain-pinecone
langchain-text-splitters
pandas
pinecone[grpc]
pypdf
python-dotenv
requests
sentence-transformers
streamlit
uvicorn